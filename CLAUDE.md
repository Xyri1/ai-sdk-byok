# CLAUDE.md

The source of truth for this project's spec is **`AGENTS.md`** (in this same directory). Read it first.

This file exists only because Claude Code looks for `CLAUDE.md` by convention. It contains no unique content. Other coding agents (Cursor, Aider, Cline, etc.) may have their own thin pointer files; all of them defer to `AGENTS.md`.

If you find guidance in this file that contradicts `AGENTS.md`, `AGENTS.md` wins.
